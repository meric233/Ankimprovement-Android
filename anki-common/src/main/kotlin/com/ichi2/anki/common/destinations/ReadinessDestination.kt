// SPDX-License-Identifier: GPL-3.0-or-later

package com.ichi2.anki.common.destinations

/** Opens the USMLE study readiness dashboard (Memory + Coverage + Readiness). */
data object ReadinessDestination : Destination()

/** Opens the developer admin / simulation screen (bulk FSRS state + time travel). */
data object AdminSimulationDestination : Destination()
